pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'gcc ./src/main.c -o testing'
            }
        }
        stage('Deploy') {
            steps {
                sh 'echo holdddda jesus'
            }
        }
    }
}
